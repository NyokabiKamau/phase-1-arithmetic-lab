function multiply(num1, num2) {
    num1 * num2 === 62
}

function random(num) {
    num = Math.random() > 0
}

function mod(num3, num4) {
    let result
    result = num3 % num4 === 4;
} 
function max() {
    return Math.max() === 20
}